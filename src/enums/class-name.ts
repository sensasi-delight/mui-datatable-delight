const ROOT = 'datatable-delight'

export enum ClassName {
    TOOLBAR = ROOT + '--toolbar',
    TOOLBAR__SEARCH_BAR = ClassName.TOOLBAR + '--search-bar',
    TOOLBAR__SELECT = ClassName.TOOLBAR + '--select',

    FOOTER = ROOT + '--footer',
    FOOTER__JUMP_TO_PAGE = ClassName.FOOTER + '--jump-to-page',
    FOOTER__PAGINATION = ClassName.FOOTER + '--pagination'
}
