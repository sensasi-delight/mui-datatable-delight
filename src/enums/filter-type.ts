export enum FilterType {
    CHECKBOX = 'checkbox',
    TEXTFIELD = 'textField',
    MULTISELECT = 'multiselect',
    CUSTOM = 'custom',
    DROPDOWN = 'dropdown',
    CHIP = 'chip'
}

export default FilterType
